{
  "routes": [{ "src": "/(.*)", "dest": "app.js" }],
  "buildCommand": "npm run build",
  "builds": [
    {
      "src": "app.js",
      "use": "@vercel/node"
    }
  ],
  "outputDirectory": "dist"
}
